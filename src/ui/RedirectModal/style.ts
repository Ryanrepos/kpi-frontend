import { styled } from 'styled-components';

export const StyledRedirectModal = styled.div`
  .content {
    text-align: center;
    padding: 24px 0 32px 0;
    font-size: 0.888888rem;
    font-weight: 400;

    margin-top: 60px;
  }

  .countdown {
    position: absolute;
    top: 30px;
    left: 30px;
    display: flex;
    align-items: center;
    gap: 20px;
    font-size: 0.888888rem;
    font-weight: 400;
  }
`;
